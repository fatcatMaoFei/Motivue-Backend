# Backend settings (local SQLite by default)
DATABASE_URL=sqlite:////data/local.db

# Optional: leave empty for offline heuristic weekly report
GOOGLE_API_KEY=
READINESS_LLM_MODEL=
READINESS_LLM_FALLBACK_MODELS=

# Service URLs used internally
BASELINE_SERVICE_URL=http://baseline:8001
RABBITMQ_URL=
